# PostgreSQL connection string (local instance via docker compose)
DATABASE_URL="postgresql://aistos:aistos@localhost:5432/debts"

# HTTP server port
PORT=3000

# AES-256-GCM encryption key (base64, 32 bytes) for name/email at rest
# Generate with: bun -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
ENCRYPTION_KEY=

# Stripe test mode
STRIPE_SECRET_KEY=sk_test_xxx
PUBLIC_BASE_URL=http://localhost:3000
STRIPE_WEBHOOK_SECRET=whsec_xxx